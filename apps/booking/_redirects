# Cloudflare Pages Redirects Configuration

# API routes proxy to Cloudflare Worker (proxy 200)
# Route all /api/* requests to the Worker backend so Pages serves frontend and Worker handles API.
/api/* https://appointmentbooking-coza.houseofgr8ness.workers.dev/api/:splat 200

# Legacy tenant routes
/instylehairboutique/* /instyle/:splat 301
/instyle-hair-boutique/* /instyle/:splat 301

# Root redirect to main tenant
/ /instyle 301